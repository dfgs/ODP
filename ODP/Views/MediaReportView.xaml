<UserControl x:Class="ODP.Views.MediaReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:ODP.Views"
             mc:Ignorable="d" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
             d:DesignWidth="300">
    <Border BorderThickness="1" BorderBrush="#55000000" Background="#55FFFFFF">
		<StackPanel>
			<TextBlock Text="{Binding RTPdelay,StringFormat=Delay: {0} ms}"/>
			<TextBlock Text="{Binding RTPjitter,StringFormat=Jitter: {0} ms}"/>
			<TextBlock Text="{Binding LocalPackLoss,StringFormat=Packet loss: {0}}"/>
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="1*"/>
					<ColumnDefinition Width="1*"/>
				</Grid.ColumnDefinitions>
				<StackPanel Grid.Column="0">
					<TextBlock Text="Local RTP:" TextDecorations="Underline"/>
					<TextBlock Text="{Binding LocalRtpIp}"/>
					<TextBlock Text="{Binding LocalRtpPort}"/>
				</StackPanel>
				<StackPanel Grid.Column="1">
					<TextBlock Text="Remote RTP:" TextDecorations="Underline"/>
					<TextBlock Text="{Binding RemoteRtpIp}"/>
					<TextBlock Text="{Binding RemoteRtpPort}"/>
				</StackPanel>
			</Grid>
		</StackPanel>
	</Border>
</UserControl>
